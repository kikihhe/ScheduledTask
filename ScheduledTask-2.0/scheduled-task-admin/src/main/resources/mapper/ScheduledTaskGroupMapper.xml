<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaohe.mapper.ScheduledTaskGroupMapper">

    <resultMap id="ScheduledTaskGroup" type="com.xiaohe.core.model.ScheduledTaskGroup" >
        <result column="id" property="id" />
        <result column="app_name" property="appname" />
        <result column="title" property="title" />
        <result column="address_type" property="addressType" />
        <result column="address_list" property="addressList" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <sql id="Base_Column_List">
        t.id,
		t.app_name,
		t.title,
		t.address_type,
		t.address_list,
		t.update_time
    </sql>
    <update id="update" parameterType="com.xiaohe.core.model.ScheduledTaskGroup">
        UPDATE scheduled_task_group
        SET `app_name` = #{appname},
            `title` = #{title},
            `address_type` = #{addressType},
            `address_list` = #{addressList},
            `update_time` = #{updateTime}
        WHERE id = #{id}

    </update>

    <select id="loadById" resultType="com.xiaohe.core.model.ScheduledTaskGroup">
        SELECT
        <include refid="Base_Column_List"/>
        FROM scheduled_task_group AS t
        WHERE t.id = #{id}
    </select>


    <select id="findByAddressType" parameterType="integer" resultMap="ScheduledTaskGroup">
        select <include refid="Base_Column_List"></include>
            from scheduled_task_group t
        where t.address_type = #{addressType}

    </select>
</mapper>